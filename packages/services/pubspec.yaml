name: services
description: Core service ports (economy, save/profile, telemetry) with no platform impls.
version: 0.1.0
publish_to: 'none'

environment:
     sdk: '>=3.3.0 <4.0.0'

dependencies:
     flutter:
          sdk: flutter

dev_dependencies:
     flutter_test:
          sdk: flutter
     lints: ^5.0.0
