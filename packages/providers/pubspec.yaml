name: providers
description: Provider bundles (registry, scene router, analytics streams).
version: 0.1.0
publish_to: 'none'

environment:
     sdk: '>=3.3.0 <4.0.0'

dependencies:
     flutter:
          sdk: flutter
     riverpod: ^2.5.1
     hooks_riverpod: ^2.5.1
     services:
          path: ../services

dev_dependencies:
     flutter_test:
          sdk: flutter
     lints: ^5.0.0
